<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.springboot.georlock.mapper.SignupMapper">
    <insert id="signUpis" parameterType ="com.springboot.georlock.dto.Member"
           >
        INSERT INTO MEMBER(userID, userPass, userName, userQue, userAns) VALUES(#{userID},#{userPass},#{userName},#{userQue},#{userAns});
    </insert>

</mapper>